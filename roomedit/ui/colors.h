#pragma once
#define WIN32_LEAN_AND_MEAN
#include <Windows.h>

namespace Colors
{
	const auto DefaultBackground = RGB(47, 52, 63);

	const auto DefaultText = RGB(248, 248, 242);

	const auto DefaultHilite = RGB(98, 114, 164);

	//buttons
	const auto DefaultButtonBackground = RGB(68, 71, 90);
	const auto DefaultButtonText = DefaultText;
}
